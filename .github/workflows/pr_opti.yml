name: Check images

on:
  workflow_dispatch:
  pull_request_target:

permissions:
  contents: write
  pull-requests: write

jobs:
  check-images:
    name: PNG compression check
    uses: ./.github/workflows/optimize.yml
    secrets: inherit
